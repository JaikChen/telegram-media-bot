version: "3.9"

services:
  telegram-bot:
    build: .
    container_name: telegram-media-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # 挂载数据库目录，持久化存储
    networks:
      - botnet

networks:
  botnet:
    driver: bridge